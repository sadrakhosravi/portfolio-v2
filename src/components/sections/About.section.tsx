import React from 'react';

// Components
import SectionTitle from '../SectionTitle.Component';

const AboutSection = () => {
  return (
    <section>
      <SectionTitle />
    </section>
  );
};
export default AboutSection;
